# Copy this file to .env for local development and set your values.
# Do NOT commit real secrets. The deployment environment should set these variables.

# Required for OpenAI access
OPENAI_API_KEY=your_openai_api_key_here

# Optional overrides
OPENAI_MODEL=gpt-4o-mini
# OPENAI_API_BASE=https://api.openai.com/v1  # default; override if using proxy/Azure-compatible endpoint
